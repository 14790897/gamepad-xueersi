# Project Guidelines

## 包与依赖
- **优先使用现有库**：如果已有库能提供相同功能，必须使用现有库而非从零编写。
- **保持简洁**：生成的代码不应包含任何不必要的功能，保持简单。
- **优先现代版本**：对于包和框架，始终优先使用更现代的版本。

## 信息未知时
- 当相关信息未知时，必须搜索互联网获取答案，而不是猜测或假设。

## 依赖升级
- 遇到框架/依赖升级导致 breaking change 时，先检查最新版本是否已修复，不要跳过现有方案自己造轮子。
